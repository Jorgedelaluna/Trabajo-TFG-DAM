server:
  port: 8080

spring:
  application:
    name: crossfit-manager-app

  datasource:
    url: jdbc:mysql://localhost:3306/crossfit_tfg?useSSL=false&serverTimezone=UTC
    username: root
    password: Nala.1122   # Poner contraseña real
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # Opciones: none, validate, update, create, create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

  sql:
    init:
      mode: never

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

jwt:
  secret: superSecretKeyForTFG
  expiration: 3600000   # 1 hora en milisegundos